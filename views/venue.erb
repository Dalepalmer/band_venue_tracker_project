<h1> Dale's Delightful band and tour tracker </h1>

<h2> Add venues where <%= @band.name() %> has played </h2>
<h3> Where they have already played </h3>
<% if @band_venues.any? %>
<% @band_venues.each() do |band_venue| %>
  <li><%= band_venue.name() %> </a></li>
  <% end %>
  <% else %>

  No venues have been added to <%= @band.name() %>'s record yet </h2>
  <% end %>

<h2> Here is the list of venues to choose from : </h2>
<% if @venues.any? %>
<% @venues.each() do |venue| %>
<form action="/bands_venue_add" method="post">
  <div class="checkbox">
    <label>
      <input type="checkbox" id="venue_ids_array[]" name="venue_ids_array[]" value="<%= venue.id() %>">
      <input type="hidden" id="band_id" name="band_id" value="<%=@band.id() %>">
      <li><p "/venues/<%= venue.id %>"> <%= venue.name() %></a></li>
      </label>
    </div>
    <% end %>
    <button type="submit" class="btn btn-warning"> Add the venues to this band</button>
  </form>


  <% else %>



  No venues have been added yet.
  <% end %>




  <h2> List the name of a venue </h2>

  <form action="/venues" method="post">
    <label for="venue_name"> Name of the venue </label>
    <input type="text" name="venue_name" id="venue_name" placeholder="CBGY's">
    <button class="button" type="submit"> Add the venue! </button>
  </form>
